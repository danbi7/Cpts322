<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wsu.crimsonconnect.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.wsu.crimsonconnect.domain.User">
        INSERT INTO users (first_name, last_name, username, email, password_hash)
        VALUES (#{firstName}, #{lastName}, #{username}, #{email}, #{passwordHash});
    </insert>

    <select id="findByUsername" parameterType="string" resultType="com.wsu.crimsonconnect.domain.User">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="findByEmail" parameterType="string" resultType="com.wsu.crimsonconnect.domain.User">
        SELECT * FROM users WHERE email = #{email}
    </select>
</mapper>
